<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#41414C" />
    <link rel="stylesheet" href="/styles/global.css">
    <link rel="stylesheet" href="/styles/index.css">
    <link rel="stylesheet" href="/styles/novo.css">
    <link rel="stylesheet" href="/styles/contato.css">
    <link rel="stylesheet" href="/styles/header.css">

    <title>Cadastro de Clientes</title>
</head>

<body>
    <div class="container">
        <%- include('parts/header', {title: 'Contatos' , hasBack: true , button: 'Home' , urlButton: '/' }) %>
    </div>

    <div class="client-list">
        <form id="form-client-contact" method="POST">
            <div class="client-contact">

                <div class="separator light"></div>
                
                <div class="inputs-contact">
                    <span>
                        <label for="tipo">Tipo</label>
                        <select name="tipo" id="tipo" required>
                            <option value=""></option>
                            <option value="comercial">Comercial</option>
                            <option value="correspondencia">Correspondência</option>
                            <option value="residencial">Residencial</option>
                        </select>
                    </span>
                    <span>
                        <label for="ddd">DDD</label>
                        <input type="text" id="ddd" name="ddd" maxlength="2" />
                    </span>
                    <span>
                        <label for="numero">Número</label>
                        <input type="text" id="numero" name="numero" maxlength="10" />
                    </span>
                </div>
                <input type="text" name="id-contato" id="id-contato">
            </div>
            <div class="separator light"></div>
            <button id="save-new-contact" type="submit" form="form-client-contact">Gravar</button>
            <button data-aux="new-reg" type="button">Novo</button>

        </form>

        <div class="table-aux">
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Tipo</th>
                        <th>DDD</th>
                        <th>Número</th>
                    </tr>
                </thead>
                <tbody>
                    <% dataContact.forEach( contact=> { %>
                        <tr data-aux="row">
                            <td data-aux="id">
                                <%= contact.id%>
                            </td>
                            <td data-aux="tipo">
                                <%= contact.tipo%>
                            </td>
                            <td data-aux="ddd">
                                <%= contact.ddd%>
                            </td>
                            <td data-aux="numero">
                                <%= contact.numero%>
                            </td>
                        </tr>
                    <% }) %>
                    </tbody>
                </table>
        </div>
        
        <script src="/scripts/contatos.js"></script>
</body>

</html>